#include <null0.h>

int main() {
  // you can do trace (adds newline)
  trace("Hello from justlog. Null0 can do printf-style formatting, which is %dX cool!", 100);

  // you can also printf
  printf("This is a printf, with no newline.");

  return 0;
}
